// Todas las colecciones (songs + quotes) migraron a D1 (apps/api).
// El frontend las fetchea en build time desde /api/songs y /api/quotes.
// Si en el futuro hay contenido que prefiramos en markdown del repo
// (ej. press_releases, posts), se define acá.
export const collections = {};
